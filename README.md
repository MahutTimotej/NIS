NIS – Hospital Information System
Student semester project focused on a simple hospital information system.

---

Requirements
To run this project, the following tools and services are required:

.NET SDK 8.0
Visual Studio 2022 or newer
Oracle Database XE
Oracle SQL Developer (recommended)
Web browser (Chrome, Edge, Firefox)

---

Database Setup
Oracle XE must be running
Database service: XEPDB1
Valid Oracle listener must be active
Database schema must contain required tables and relations

Connection string is configured in:

```json
appsettings.json